name: 🌍 Viral Shorts Machine v5.1
on:
  schedule:
    - cron: '0 7,10,13,16,19,22 * * *'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: sudo apt-get update -qq && sudo apt-get install -y ffmpeg fonts-liberation fonts-dejavu-core
      - run: pip install -r requirements.txt
      - name: Run
        env:
          GROQ_API_KEY:            ${{ secrets.GROQ_API_KEY }}
          PEXELS_API_KEY:          ${{ secrets.PEXELS_API_KEY }}
          PIXABAY_API_KEY:         ${{ secrets.PIXABAY_API_KEY }}
          YT_CLIENT_ID:            ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET:        ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN:        ${{ secrets.YT_REFRESH_TOKEN }}
          YOUTUBE_PLAYLIST_ID:     ${{ secrets.YOUTUBE_PLAYLIST_ID }}
          SHEETS_DOC_ID:           ${{ secrets.SHEETS_DOC_ID }}
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          LOGO_B64:                ${{ secrets.LOGO_B64 }}
        run: python main.py
